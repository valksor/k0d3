interface:
  display_name: Using Git Worktrees
  short_description: feature work needs isolation, or before executing an implementation
    plan
  default_prompt: Use $using-git-worktrees — feature work needs isolation, or before
    executing an implementation plan
