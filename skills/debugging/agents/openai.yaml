interface:
  display_name: Debugging
  short_description: any bug, test failure, or unexpected behavior before proposing
    fixes
  default_prompt: Use $debugging — any bug, test failure, or unexpected behavior before
    proposing fixes
